services:
  - type: web
    name: carrot-game
    runtime: static
    buildCommand: "true"
    staticPublishPath: .
    previews:
      generation: manual
    renderSubdomainPolicy: enabled
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
